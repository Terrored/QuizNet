@model QuizNet.BusinessLogic.DTOs.QuestionDto
@{
    ViewData["Title"] = "Get";
}

<h1>@Model.Text</h1>
<p class="mb-0"><small><em>Created at @Model.CreationTime</em></small></p>
<br />
<ul>
    @*@for (int i = 0; i < Model.Answers.Length; i++)
    {
        if (i == Model.CorrectAnswerId)
        {
            <li class="list-group-item active">@Model.Answers[i].Text</li>
        }
        else
        {
            <li class="list-group-item">@Model.Answers[i].Text</li>
        }
    }*@
    
    @foreach (var answer in Model.Answers)
    {
        if (answer.Id == Model.CorrectAnswer.Id)
        {
            <li class="list-group-item active">@answer.Text</li>
        }
        else
        {
            <li class="list-group-item">@answer.Text</li>
        }
    }
</ul>

